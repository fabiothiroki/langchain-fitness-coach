services:
  fitness-coach:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./coach.db:/app/coach.db
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
      - GRADIO_SERVER_PORT=7860
